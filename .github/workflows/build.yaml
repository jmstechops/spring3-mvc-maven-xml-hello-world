name:  build and deploy

on:
  push:
    branches: [ master ]
  

jobs:
  build:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v4
    - name: Build with Maven
      run: mvn package
